# Live network data — never commit. These contain MACs, IPs, device names,
# floor-plan geometry and console identifiers from a real deployment.
project.json
system.json
*.csv
*.zip
maps/
images/

# Python
__pycache__/
*.pyc

# Schedule app — local secrets and data
schedule_app/.env
schedule_app/*.db
